let name = "huSSain";
let full_name = name.charAt(0).toUpperCase();
for (let i=1; i<name.length; i++)
{
  full_name += name[i].toLowerCase();
  
}
console.log(name.toUpperCase());
console.log(name.toLowerCase());
console.log(full_name)
